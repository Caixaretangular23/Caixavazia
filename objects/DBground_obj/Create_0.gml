itime = 1; // imagetime é a determinação de imagem no sprite
ispin = false; // Estar girando?
ian = 0; // ângulo dinâmico das imagens

// Linha X

Ly = 0;

Bg = false; // Começa...

// Começa automaticamente

if (instance_exists(Controll_obj)) if (Controll_obj.Gm) itime = 0;